export declare class Theme {
  /** Set the brand name that will set the theme styling for the page. */
  name: string;
  mode: string;
  el: HTMLElement;
  tagName: string;
  currentTheme: {
    favicons: any[];
    components: any[];
  };
  favicons: string[];
  store: {
    theme: {
      current: string;
      items: {};
    };
    navigation: {
      open: boolean;
      expanded: any;
    };
  };
  setName(mode: any): void;
  setTheme(mode?: any): void;
  renderFavicon(): void;
  componentWillLoad(): void;
  render(): any[];
}
